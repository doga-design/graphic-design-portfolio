{
  "scripts": {
    "build:icons": "node scripts/build-icons.mjs",
    "local": "npx vercel dev",
    "env:pull": "npx vercel env pull .env.local --environment=development --yes"
  },
  "dependencies": {
    "@untitledui/icons": "^0.0.22",
    "geist": "^1.7.1"
  },
  "devDependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
